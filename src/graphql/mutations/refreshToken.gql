mutation refreshToken ($id: Float!, $refreshToken: String!) {
    refreshToken(id: $id, refreshToken: $refreshToken) {
        accessToken
        refreshToken
        user {
            id
            username
            email
            isVerified
        }
    }
}